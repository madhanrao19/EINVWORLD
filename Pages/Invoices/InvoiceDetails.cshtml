@page "{uuid}"
@model eInvWorld.Pages.Invoices.InvoiceDetailsModel
@{
    // DEPRECATED page — the handler always redirects to InvoiceDetails2, so this markup never renders.
    // Retained only so old email links to /Invoices/InvoiceDetails/{uuid} continue to resolve.
    Layout = null;
}
